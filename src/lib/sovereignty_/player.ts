enum Move {
    cooperate,
    defect
}
export class Player {
    // 🪄

    _myMoves?: Move[];
    _oppositions?: Move[];
    
    _defectFirst?: boolean;


    // I will
    defectFirst() {
        this._defectFirst = true;
        return new defectFirst;
    }
}

export class defectFirst {
    onLine(n: number) {
        // 🪄
    }
}